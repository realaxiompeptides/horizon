{% comment %}
  Retained as an empty compatibility section because older header-group data can
  still reference it. Bundle price updates now live in 00-avonent-price-offer,
  so loading a second timer-based synchronizer only adds duplicate client work.
{% endcomment %}

{% schema %}
{
  "name": "Moringa price sync",
  "enabled_on": {
    "groups": ["header"]
  },
  "settings": [],
  "presets": [
    {"name":"Moringa price sync"}
  ]
}
{% endschema %}
