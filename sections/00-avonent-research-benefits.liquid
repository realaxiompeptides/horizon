{% liquid
  assign section_id = 'AvonentResearchBenefits-' | append: section.id
%}

<section
  id="{{ section_id }}"
  class="avonent-research-benefits"
  style="
    --arb-bg: {{ section.settings.background_color }};
    --arb-card-bg: {{ section.settings.card_background }};
    --arb-text: {{ section.settings.text_color }};
    --arb-muted: {{ section.settings.muted_text_color }};
    --arb-accent: {{ section.settings.accent_color }};
    --arb-border: {{ section.settings.border_color }};
    --arb-width: {{ section.settings.content_width }}px;
    --arb-gap: {{ section.settings.card_gap }}px;
    --arb-height-desktop: {{ section.settings.card_height_desktop }}px;
    --arb-height-mobile: {{ section.settings.card_height_mobile }}px;
    --arb-top: {{ section.settings.padding_top_desktop }}px;
    --arb-bottom: {{ section.settings.padding_bottom_desktop }}px;
    --arb-top-mobile: {{ section.settings.padding_top_mobile }}px;
    --arb-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;
  "
>
  <div class="avonent-research-benefits__inner">
    <header class="avonent-research-benefits__header">
      <h2 class="avonent-research-benefits__heading">
        {% if section.settings.heading_accent != blank %}<span>{{ section.settings.heading_accent }}</span>{% endif %}
        {% if section.settings.heading_rest != blank %} {{ section.settings.heading_rest }}{% endif %}
      </h2>
      {% if section.settings.subheading != blank %}
        <div class="avonent-research-benefits__subheading rte">{{ section.settings.subheading }}</div>
      {% endif %}
    </header>

    <div class="avonent-research-benefits__grid" role="list">
      {% for block in section.blocks %}
        <div class="avonent-research-benefits__card" role="listitem" {{ block.shopify_attributes }}>
          <button
            class="avonent-research-benefits__button"
            type="button"
            aria-expanded="false"
            aria-label="Open {{ block.settings.title | escape }} details"
            data-arb-card
          >
            <span class="avonent-research-benefits__flipper">
              <span class="avonent-research-benefits__face avonent-research-benefits__face--front" data-arb-front>
                <span class="avonent-research-benefits__visual" aria-hidden="true">
                  {% if block.settings.image != blank %}
                    {{ block.settings.image | image_url: width: 360 | image_tag: loading: 'lazy', class: 'avonent-research-benefits__image', alt: '' }}
                  {% else %}
                    <span class="avonent-research-benefits__fallback-icon">{{ block.settings.fallback_icon }}</span>
                  {% endif %}
                </span>

                <span class="avonent-research-benefits__front-copy">
                  <strong class="avonent-research-benefits__title">{{ block.settings.title }}</strong>
                  <span class="avonent-research-benefits__line">{{ block.settings.front_line }}</span>
                </span>

                <span class="avonent-research-benefits__toggle" aria-hidden="true">+</span>
              </span>

              <span class="avonent-research-benefits__face avonent-research-benefits__face--back" aria-hidden="true" data-arb-back>
                <span class="avonent-research-benefits__back-copy">
                  <strong class="avonent-research-benefits__back-title">{{ block.settings.title }}</strong>
                  <span class="avonent-research-benefits__back-text">{{ block.settings.back_copy }}</span>
                </span>

                <span class="avonent-research-benefits__toggle" aria-hidden="true">−</span>
              </span>
            </span>
          </button>
        </div>
      {% endfor %}
    </div>

    {% if section.settings.footnote != blank %}
      <div class="avonent-research-benefits__footnote rte">{{ section.settings.footnote }}</div>
    {% endif %}
  </div>
</section>

{% stylesheet %}
  .avonent-research-benefits {
    padding: var(--arb-top) 0 var(--arb-bottom);
    background: var(--arb-bg);
    color: var(--arb-text);
  }

  .avonent-research-benefits__inner {
    width: min(calc(100% - 40px), var(--arb-width));
    margin: 0 auto;
  }

  .avonent-research-benefits__header {
    width: min(100%, 760px);
    margin: 0 auto 34px;
    text-align: center;
  }

  .avonent-research-benefits__heading {
    margin: 0;
    color: var(--arb-text);
    font-size: clamp(30px, 3vw, 44px);
    font-weight: 800;
    line-height: 1.06;
    letter-spacing: -.035em;
  }

  .avonent-research-benefits__heading span {
    color: var(--arb-accent);
  }

  .avonent-research-benefits__subheading {
    margin: 13px auto 0;
    color: var(--arb-muted);
    font-size: 16px;
    line-height: 1.45;
  }

  .avonent-research-benefits__subheading p,
  .avonent-research-benefits__footnote p {
    margin: 0;
  }

  .avonent-research-benefits__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--arb-gap);
  }

  .avonent-research-benefits__card {
    min-width: 0;
    height: var(--arb-height-desktop);
    perspective: 1200px;
  }

  .avonent-research-benefits__button {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    border: 0;
    background: transparent;
    color: inherit;
    font: inherit;
    text-align: left;
    cursor: pointer;
  }

  .avonent-research-benefits__button:focus-visible {
    outline: 3px solid color-mix(in srgb, var(--arb-accent) 55%, white);
    outline-offset: 3px;
    border-radius: 14px;
  }

  .avonent-research-benefits__flipper {
    position: relative;
    display: block;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
    transition: transform 520ms cubic-bezier(.2,.72,.2,1);
  }

  .avonent-research-benefits__button[aria-expanded='true'] .avonent-research-benefits__flipper {
    transform: rotateX(180deg);
  }

  .avonent-research-benefits__face {
    position: absolute;
    inset: 0;
    display: grid;
    align-items: center;
    border: 1px solid var(--arb-border);
    border-radius: 14px;
    background: var(--arb-card-bg);
    box-shadow: 0 8px 28px rgb(17 24 39 / .035);
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
    overflow: hidden;
  }

  .avonent-research-benefits__face--front {
    grid-template-columns: 112px minmax(0,1fr) 42px;
    gap: 18px;
    padding: 10px 20px 10px 10px;
  }

  .avonent-research-benefits__face--back {
    grid-template-columns: minmax(0,1fr) 42px;
    gap: 18px;
    padding: 24px 22px 24px 28px;
    transform: rotateX(180deg);
    background: color-mix(in srgb, var(--arb-card-bg) 88%, var(--arb-accent) 12%);
  }

  .avonent-research-benefits__visual {
    display: grid;
    width: 112px;
    height: calc(var(--arb-height-desktop) - 20px);
    place-items: center;
    overflow: hidden;
    border-radius: 10px;
    background: color-mix(in srgb, var(--arb-accent) 10%, white);
  }

  .avonent-research-benefits__image {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .avonent-research-benefits__fallback-icon {
    display: grid;
    width: 52px;
    height: 52px;
    place-items: center;
    color: var(--arb-accent);
    background: white;
    border: 1px solid color-mix(in srgb, var(--arb-accent) 25%, var(--arb-border));
    border-radius: 50%;
    font-size: 24px;
    font-weight: 800;
  }

  .avonent-research-benefits__front-copy,
  .avonent-research-benefits__back-copy {
    display: flex;
    min-width: 0;
    flex-direction: column;
    justify-content: center;
  }

  .avonent-research-benefits__title,
  .avonent-research-benefits__back-title {
    color: var(--arb-text);
    font-size: 21px;
    font-weight: 750;
    line-height: 1.15;
    letter-spacing: -.02em;
  }

  .avonent-research-benefits__line {
    margin-top: 5px;
    color: var(--arb-muted);
    font-size: 16px;
    line-height: 1.35;
  }

  .avonent-research-benefits__back-text {
    margin-top: 8px;
    color: var(--arb-muted);
    font-size: 15px;
    line-height: 1.48;
  }

  .avonent-research-benefits__toggle {
    display: grid;
    width: 34px;
    height: 34px;
    place-items: center;
    justify-self: end;
    color: var(--arb-muted);
    border: 1px solid var(--arb-border);
    border-radius: 50%;
    background: white;
    font-size: 22px;
    font-weight: 400;
    line-height: 1;
  }

  .avonent-research-benefits__footnote {
    width: min(100%, 860px);
    margin: 18px auto 0;
    color: var(--arb-muted);
    font-size: 11px;
    line-height: 1.45;
    text-align: center;
  }

  @media screen and (max-width: 749px) {
    .avonent-research-benefits {
      padding: var(--arb-top-mobile) 0 var(--arb-bottom-mobile);
    }

    .avonent-research-benefits__inner {
      width: min(calc(100% - 28px), var(--arb-width));
    }

    .avonent-research-benefits__header {
      margin-bottom: 24px;
    }

    .avonent-research-benefits__heading {
      font-size: 31px;
    }

    .avonent-research-benefits__subheading {
      font-size: 15px;
    }

    .avonent-research-benefits__grid {
      grid-template-columns: 1fr;
    }

    .avonent-research-benefits__card {
      height: var(--arb-height-mobile);
    }

    .avonent-research-benefits__face--front {
      grid-template-columns: 82px minmax(0,1fr) 36px;
      gap: 12px;
      padding: 8px 12px 8px 8px;
    }

    .avonent-research-benefits__visual {
      width: 82px;
      height: calc(var(--arb-height-mobile) - 16px);
    }

    .avonent-research-benefits__face--back {
      grid-template-columns: minmax(0,1fr) 36px;
      gap: 10px;
      padding: 18px 14px 18px 18px;
    }

    .avonent-research-benefits__title,
    .avonent-research-benefits__back-title {
      font-size: 18px;
    }

    .avonent-research-benefits__line,
    .avonent-research-benefits__back-text {
      font-size: 14px;
    }

    .avonent-research-benefits__toggle {
      width: 32px;
      height: 32px;
      font-size: 20px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .avonent-research-benefits__flipper {
      transition: none;
    }
  }
{% endstylesheet %}

<script>
  (() => {
    const section = document.getElementById('{{ section_id }}');
    if (!section || section.dataset.flipReady === 'true') return;
    section.dataset.flipReady = 'true';

    const cards = Array.from(section.querySelectorAll('[data-arb-card]'));

    const closeCard = (card) => {
      card.setAttribute('aria-expanded', 'false');
      const front = card.querySelector('[data-arb-front]');
      const back = card.querySelector('[data-arb-back]');
      if (front) front.setAttribute('aria-hidden', 'false');
      if (back) back.setAttribute('aria-hidden', 'true');
    };

    const openCard = (card) => {
      cards.forEach((other) => {
        if (other !== card) closeCard(other);
      });
      card.setAttribute('aria-expanded', 'true');
      const front = card.querySelector('[data-arb-front]');
      const back = card.querySelector('[data-arb-back]');
      if (front) front.setAttribute('aria-hidden', 'true');
      if (back) back.setAttribute('aria-hidden', 'false');
    };

    cards.forEach((card) => {
      card.addEventListener('click', () => {
        const isOpen = card.getAttribute('aria-expanded') === 'true';
        if (isOpen) closeCard(card);
        else openCard(card);
      });
    });
  })();
</script>

{% schema %}
{
  "name": "Avonent benefit flips",
  "tag": "section",
  "class": "section-avonent-benefit-flips",
  "max_blocks": 6,
  "settings": [
    {"type":"header","content":"Heading"},
    {"type":"text","id":"heading_accent","label":"Accent heading text","default":"Real Benefits."},
    {"type":"text","id":"heading_rest","label":"Heading text","default":"Backed by Research."},
    {"type":"richtext","id":"subheading","label":"Subheading","default":"<p>One plant. Multiple reasons people take it every day.</p>"},
    {"type":"richtext","id":"footnote","label":"Footnote","default":"<p>† These statements have not been evaluated by the Food and Drug Administration. This product is not intended to diagnose, treat, cure, or prevent any disease.</p>"},

    {"type":"header","content":"Layout"},
    {"type":"range","id":"content_width","label":"Content width","min":800,"max":1400,"step":20,"unit":"px","default":1200},
    {"type":"range","id":"card_gap","label":"Card gap","min":8,"max":40,"step":2,"unit":"px","default":14},
    {"type":"range","id":"card_height_desktop","label":"Desktop card height","min":120,"max":260,"step":10,"unit":"px","default":150},
    {"type":"range","id":"card_height_mobile","label":"Mobile card height","min":120,"max":260,"step":10,"unit":"px","default":150},
    {"type":"range","id":"padding_top_desktop","label":"Desktop spacing above","min":0,"max":140,"step":4,"unit":"px","default":52},
    {"type":"range","id":"padding_bottom_desktop","label":"Desktop spacing below","min":0,"max":140,"step":4,"unit":"px","default":52},
    {"type":"range","id":"padding_top_mobile","label":"Mobile spacing above","min":0,"max":100,"step":4,"unit":"px","default":36},
    {"type":"range","id":"padding_bottom_mobile","label":"Mobile spacing below","min":0,"max":100,"step":4,"unit":"px","default":36},

    {"type":"header","content":"Colors"},
    {"type":"color","id":"background_color","label":"Section background","default":"#F7F8F2"},
    {"type":"color","id":"card_background","label":"Card background","default":"#FFFFFF"},
    {"type":"color","id":"text_color","label":"Heading text","default":"#111111"},
    {"type":"color","id":"muted_text_color","label":"Body text","default":"#4F5965"},
    {"type":"color","id":"accent_color","label":"Accent","default":"#02C6EA"},
    {"type":"color","id":"border_color","label":"Card border","default":"#D8DEE2"}
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Benefit card",
      "settings": [
        {"type":"image_picker","id":"image","label":"Image"},
        {"type":"text","id":"fallback_icon","label":"Fallback icon","default":"✦"},
        {"type":"text","id":"title","label":"Title","default":"Daily Energy"},
        {"type":"text","id":"front_line","label":"Front line","default":"Wake up with real energy. Stay steady all day."},
        {"type":"textarea","id":"back_copy","label":"Back copy","default":"Moringa leaf is packed with plant nutrients your body uses for everyday energy — so you feel fueled without relying on another coffee."}
      ]
    }
  ],
  "presets": [
    {
      "name": "Avonent benefit flips",
      "blocks": [
        {"type":"benefit","settings":{"fallback_icon":"⚡","title":"Daily Energy","front_line":"Wake up with real energy. Stay steady all day.","back_copy":"Moringa leaf is packed with plant nutrients your body uses for everyday energy — so you feel fueled without relying on another coffee."}},
        {"type":"benefit","settings":{"fallback_icon":"↗","title":"Joint Comfort","front_line":"Move easier. Recover like you used to.","back_copy":"Used for generations as a daily botanical for movement. Modern interest focuses on joint comfort and everyday mobility support.†"}},
        {"type":"benefit","settings":{"fallback_icon":"⇄","title":"Metabolic Wellness","front_line":"Support the way your body handles energy.","back_copy":"Human studies have explored moringa and markers like fasting glucose and HbA1c. The research is promising and still growing.†"}},
        {"type":"benefit","settings":{"fallback_icon":"♥","title":"Heart Wellness","front_line":"Daily support for a healthier heart routine.","back_copy":"Moringa’s nutrient and antioxidant profile is studied for cardiometabolic wellness — a simple daily add-on for heart-healthy habits.†"}},
        {"type":"benefit","settings":{"fallback_icon":"✦","title":"Healthy Aging","front_line":"Feel stronger as the years add up.","back_copy":"Antioxidant plant compounds help support the body against everyday oxidative stress — the kind that adds up over time.†"}},
        {"type":"benefit","settings":{"fallback_icon":"◇","title":"Immune Defense","front_line":"Nutritional backup for everyday immune function.","back_copy":"Vitamins, minerals, and phytonutrients in moringa leaf help support normal immune function as part of a daily wellness routine.†"}}
      ]
    }
  ]
}
{% endschema %}
